spring:
  cloud:
    stream:
      binders:
        kafka-local:
          type: kafka
          environment:
            spring.cloud.stream.kafka.binder:
              brokers: kafka:9092
              configuration:
                security.protocol: PLAINTEXT